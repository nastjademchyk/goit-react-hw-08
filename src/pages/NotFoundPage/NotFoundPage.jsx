import React from "react";
import { NavLink } from "react-router-dom";
import s from "./NoyFoundPage.module.css";

const NotFoundPage = () => {
  return <div className={s.text}>Page is not found...</div>;
};

export default NotFoundPage;
